// Activity 4:
// Create a variable called num. 
// Check if the variable is divisible by 3 or 5. If it 
// is, log “This number is divisible by 3 or 5”. 
// Otherwise log something else. 


let num=30                                      //change num to test
if (((num % 3) == 0) || ((num % 5) == 0)) {
    console.log(`${num} is divisble by 3 or 5`);
}
else {
    console.log(`${num} is not divisble by 3 or 5`);
}